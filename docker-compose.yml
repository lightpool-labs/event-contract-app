services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: event_app
      POSTGRES_PASSWORD: event_app
      POSTGRES_DB: event_contract_app
    ports:
      - "5432:5432"
    volumes:
      - event_contract_pg:/var/lib/postgresql/data

volumes:
  event_contract_pg:
