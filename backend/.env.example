HOST=0.0.0.0
PORT=3001
CLOB_INDEX_URL=http://127.0.0.1:3002
DATABASE_URL=postgres://event_app:event_app@127.0.0.1:5432/event_contract_app
# Base64-encoded dev signer secret key. Set this to keep the same address across restarts.
# DEV_SECRET_KEY=
# Collateral token contract address (e.g. USDT). Set this to keep balances across restarts.
# CASH_TOKEN_ADDRESS=
# CASH_TOKEN_SYMBOL=USDT
