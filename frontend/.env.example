NEXT_PUBLIC_API_URL=http://127.0.0.1:3001/api
NEXT_PUBLIC_EVM_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_EVM_CHAIN_ID=1337
# Hex private key for the MetaMask address (same key). Needed because LightPool
# tx digests cannot be signed with MetaMask personal_sign. Must match USER_ETH.
# NEXT_PUBLIC_LP_PRIVATE_KEY=0x...
